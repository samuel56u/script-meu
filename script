#!/data/data/com.termux/files/usr/bin/bash

# ============================================================================
# XMRIG MINER GOD - TERMUX ULTIMATE EDITION v3000.0 - FUNCIONAL
# ============================================================================
#   ██╗  ██╗███╗   ███╗██████╗ ██╗ ██████╗     ██████╗  ██████╗ ██████╗ 
#   ╚██╗██╔╝████╗ ████║██╔══██╗██║██╔════╝    ██╔════╝ ██╔═══██╗██╔══██╗
#    ╚███╔╝ ██╔████╔██║██████╔╝██║██║         ██║  ███╗██║   ██║██║  ██║
#    ██╔██╗ ██║╚██╔╝██║██╔══██╗██║██║         ██║   ██║██║   ██║██║  ██║
#   ██╔╝ ██╗██║ ╚═╝ ██║██║  ██║██║╚██████╗    ╚██████╔╝╚██████╔╝██████╔╝
#   ╚═╝  ╚═╝╚═╝     ╚═╝╚═╝  ╚═╝╚═╝ ╚═════╝     ╚═════╝  ╚═════╝ ╚═════╝ 
# ============================================================================
#   🔥 VERSÃO CORRIGIDA E TOTALMENTE FUNCIONAL – SEM BUGS 🔥
# ============================================================================

# ----------------------------------------------------------------------------
# CONFIGURAÇÕES FIXAS (ALTERE SE NECESSÁRIO)
# ----------------------------------------------------------------------------
WALLET_ADDRESS="bc1qtz2j3ky2nh409w432wh8uyqphhny84a3qhs2tl"
WEBHOOK_DISCORD="https://discord.com/api/webhooks/1470009252063088661/1j4kNu2sDcGQmFCywnbd_p1BEbb1DTcN_VXt5OYpX8_FsQI2YKB3tCjS2Okp_msUX9Mh"
AUTO_POOL=true
POOL_INTERVAL=1800            # 30 minutos
WATCHDOG=true
TLS_ENABLED=false
DONATE_LEVEL=0
CPU_THREADS=$(($(nproc 2>/dev/null || echo 2) - 1))
[ $CPU_THREADS -lt 1 ] && CPU_THREADS=1
CPU_PRIORITY=5
CPU_MAX=75

# ----------------------------------------------------------------------------
# VARIÁVEIS GLOBAIS
# ----------------------------------------------------------------------------
SYSTEM_VERSION="3000.0"
MINER_ROOT="/data/data/com.termux/files/home/.xmrig_god_final"
MINER_BIN="$MINER_ROOT/bin"
MINER_CONFIG="$MINER_ROOT/config"
MINER_LOGS="$MINER_ROOT/logs"
MINER_STATS="$MINER_ROOT/stats"
MINER_SCRIPTS="$MINER_ROOT/scripts"
MINER_TEMP="$MINER_ROOT/temp"
MINER_BACKUP="$MINER_ROOT/backup"
mkdir -p "$MINER_BIN" "$MINER_CONFIG" "$MINER_LOGS" "$MINER_STATS" "$MINER_SCRIPTS" "$MINER_TEMP" "$MINER_BACKUP"

LOG_MAX_SIZE=10485760  # 10MB
touch "$MINER_LOGS"/{system.log,miner.log,error.log,discord.log,satoshi.log,hashrate.log,xmrig_raw.log,pool.log,watchdog.log,auto_pool.log,install.log}

# Cores
NC='\033[0m'; RED='\033[0;31m'; GREEN='\033[0;32m'; YELLOW='\033[1;33m'; CYAN='\033[0;36m'; PURPLE='\033[0;35m'

# ----------------------------------------------------------------------------
# LISTA DE POOLS (APENAS AS MAIS CONFIÁVEIS)
# ----------------------------------------------------------------------------
POOLS=(
    "randomxmonero.auto.nicehash.com:9200"
    "randomxmonero.eu.nicehash.com:3380"
    "randomxmonero.usa.nicehash.com:3380"
    "pool.supportxmr.com:3333"
    "pool.supportxmr.com:443"
    "gulf.moneroocean.stream:10001"
    "pool.moneroocean.stream:10001"
    "asia.moneroocean.stream:10001"
    "us.moneroocean.stream:10001"
    "eu.moneroocean.stream:10001"
)

# ----------------------------------------------------------------------------
# FUNÇÕES AUXILIARES
# ----------------------------------------------------------------------------

rotate_log() {
    local logfile="$1"
    [ -f "$logfile" ] && [ $(wc -c < "$logfile" 2>/dev/null || echo 0) -gt $LOG_MAX_SIZE ] && {
        mv "$logfile" "$logfile.old"
        touch "$logfile"
    }
}

log() {
    local level="$1"
    local msg="$2"
    local timestamp="[$(date '+%Y-%m-%d %H:%M:%S')]"
    rotate_log "$MINER_LOGS/system.log"
    echo "$timestamp [$level] $msg" >> "$MINER_LOGS/system.log"
    case "$level" in
        INFO)  echo -e "${CYAN}[INFO]${NC} $msg" ;;
        OK)    echo -e "${GREEN}[OK]${NC} $msg" ;;
        WARN)  echo -e "${YELLOW}[WARN]${NC} $msg" ;;
        ERROR) echo -e "${RED}[ERROR]${NC} $msg" ;;
        *)     echo "[$level] $msg" ;;
    esac
}

log_info()   { log "INFO" "$1"; }
log_ok()     { log "OK" "$1"; }
log_warn()   { log "WARN" "$1"; }
log_error()  { log "ERROR" "$1"; }

discord_send() {
    local msg="$1"
    local color="$2"
    local title="$3"
    [ -z "$WEBHOOK_DISCORD" ] || [[ "$WEBHOOK_DISCORD" == *"example.com"* ]] && return
    local json=$(cat <<EOF
{"embeds":[{"title":"$title","description":"$msg","color":$color,"timestamp":"$(date -u +'%Y-%m-%dT%H:%M:%S.000Z')"}]}
EOF
)
    curl -s -H "Content-Type: application/json" -X POST -d "$json" "$WEBHOOK_DISCORD" -o /dev/null
}

notify_all() {
    local msg="$1"
    local color="$2"
    local title="$3"
    discord_send "$msg" "$color" "$title"
}

detect_arch() {
    case $(uname -m) in
        aarch64|arm64) echo "arm64" ;;
        armv7l|armhf)  echo "arm32" ;;
        x86_64|amd64)  echo "x64" ;;
        *)             echo "unknown" ;;
    esac
}

verify_xmrig() {
    local bin="$1"
    [ ! -f "$bin" ] && return 1
    chmod +x "$bin" 2>/dev/null
    "$bin" --version >/dev/null 2>&1 && return 0
    return 1
}

clean_temp() { rm -rf "$MINER_TEMP"/*; }
setup_temp() { export TMPDIR="$MINER_TEMP"; mkdir -p "$TMPDIR"; chmod 700 "$TMPDIR"; }

enable_wake_lock() {
    if command -v termux-wake-lock >/dev/null; then
        termux-wake-lock
        log_ok "Wake lock ativado"
    else
        log_warn "termux-wake-lock não encontrado (instale termux-api)"
    fi
}

disable_wake_lock() {
    command -v termux-wake-unlock >/dev/null && termux-wake-unlock
}

install_deps() {
    log_info "Verificando dependências..."
    pkg update -y >/dev/null 2>&1
    local deps=(curl wget nc git cmake make libuv libhwloc openssl jq unzip tar termux-api bc)
    for pkg in "${deps[@]}"; do
        if ! command -v "$pkg" >/dev/null 2>&1 && ! pkg list-installed 2>/dev/null | grep -q "^$pkg"; then
            pkg install -y "$pkg" >/dev/null 2>&1 && log_ok "$pkg instalado"
        fi
    done
}

fix_repos() {
    log_info "Corrigindo repositórios Termux..."
    if command -v termux-change-repo >/dev/null; then
        termux-change-repo <<< "yes" >/dev/null 2>&1
    fi
    pkg install -y termux-keyring --allow-unauthenticated >/dev/null 2>&1
    pkg update -y >/dev/null 2>&1
    log_ok "Repositórios corrigidos"
}

# ----------------------------------------------------------------------------
# MÉTODOS DE INSTALAÇÃO (APENAS OS CONFIÁVEIS)
# ----------------------------------------------------------------------------

method_pkg() {
    log_info "Método 1: Instalando via pkg..."
    pkg install -y xmrig >/dev/null 2>&1
    if command -v xmrig >/dev/null 2>&1; then
        cp "$(command -v xmrig)" "$MINER_BIN/xmrig"
        log_ok "XMRig instalado via pkg"
        return 0
    fi
    return 1
}

method_compile_stable() {
    log_info "Método 2: Compilando v6.21.0..."
    pkg install -y git cmake make libuv libhwloc openssl clang binutils >/dev/null 2>&1
    cd "$MINER_TEMP"
    rm -rf xmrig
    git clone --depth=1 --branch v6.21.0 https://github.com/xmrig/xmrig.git >/dev/null 2>&1
    [ ! -d "xmrig" ] && return 1
    cd xmrig && mkdir -p build && cd build
    cmake .. -DCMAKE_BUILD_TYPE=Release > cmake.log 2>&1
    make -j$(nproc) > make.log 2>&1
    [ -f "xmrig" ] && cp xmrig "$MINER_BIN/xmrig"
    verify_xmrig "$MINER_BIN/xmrig" && log_ok "Compilação OK" && return 0
    return 1
}

method_github_release() {
    log_info "Método 3: Baixando binário do GitHub..."
    cd "$MINER_TEMP"
    local arch=$(detect_arch)
    local url
    case "$arch" in
        arm64) url="https://github.com/xmrig/xmrig/releases/download/v6.21.0/xmrig-6.21.0-linux-arm64.tar.gz" ;;
        arm32) url="https://github.com/xmrig/xmrig/releases/download/v6.21.0/xmrig-6.21.0-linux-armhf.tar.gz" ;;
        x64)   url="https://github.com/xmrig/xmrig/releases/download/v6.21.0/xmrig-6.21.0-linux-static-x64.tar.gz" ;;
        *)     return 1 ;;
    esac
    wget -q --timeout=30 --tries=3 "$url" -O xmrig.tar.gz
    if [ -f "xmrig.tar.gz" ] && tar -xzf xmrig.tar.gz 2>/dev/null; then
        local bin=$(find . -name "xmrig" -type f -executable | head -1)
        [ -n "$bin" ] && cp "$bin" "$MINER_BIN/xmrig"
        verify_xmrig "$MINER_BIN/xmrig" && log_ok "Binário GitHub OK" && return 0
    fi
    return 1
}

method_mooo_arm64() {
    log_info "Método 4: Baixando de xmrig.mooo.info (ARM64)..."
    cd "$MINER_TEMP"
    wget -q --timeout=30 --tries=3 "http://xmrig.mooo.info/xmrigARM-1.9.5-android-arm64v8.zip" -O xmrig.zip
    if [ -f "xmrig.zip" ]; then
        unzip -o xmrig.zip >/dev/null 2>&1
        local bin=$(find . -name "xmrigARM" -type f | head -1)
        [ -n "$bin" ] && cp "$bin" "$MINER_BIN/xmrig"
        verify_xmrig "$MINER_BIN/xmrig" && log_ok "Binário mooo ARM64 OK" && return 0
    fi
    return 1
}

method_mooo_arm32() {
    log_info "Método 5: Baixando de xmrig.mooo.info (ARM32)..."
    cd "$MINER_TEMP"
    wget -q --timeout=30 --tries=3 "http://xmrig.mooo.info/xmrigARM-1.9.3-android-arm32v7.zip" -O xmrig.zip
    if [ -f "xmrig.zip" ]; then
        unzip -o xmrig.zip >/dev/null 2>&1
        local bin=$(find . -name "xmrigARM" -type f | head -1)
        [ -n "$bin" ] && cp "$bin" "$MINER_BIN/xmrig"
        verify_xmrig "$MINER_BIN/xmrig" && log_ok "Binário mooo ARM32 OK" && return 0
    fi
    return 1
}

method_musl_static() {
    log_info "Método 6: Baixando binário musl..."
    cd "$MINER_TEMP"
    local arch=$(detect_arch)
    if [ "$arch" = "arm64" ]; then
        wget -q --timeout=30 --tries=3 "https://dl.bell-sw.com/static/xmrig/xmrig-musl-arm64" -O xmrig
    elif [ "$arch" = "arm32" ]; then
        wget -q --timeout=30 --tries=3 "https://dl.bell-sw.com/static/xmrig/xmrig-musl-arm32" -O xmrig
    else
        return 1
    fi
    chmod +x xmrig
    cp xmrig "$MINER_BIN/xmrig"
    verify_xmrig "$MINER_BIN/xmrig" && log_ok "Binário musl OK" && return 0
    return 1
}

method_archive_org() {
    log_info "Método 7: Baixando do archive.org..."
    cd "$MINER_TEMP"
    local arch=$(detect_arch)
    local url
    if [ "$arch" = "arm64" ]; then
        url="https://archive.org/download/xmrig-android/xmrig-arm64.zip"
    elif [ "$arch" = "arm32" ]; then
        url="https://archive.org/download/xmrig-android/xmrig-arm32.zip"
    else
        return 1
    fi
    wget -q --timeout=30 --tries=3 "$url" -O xmrig.zip
    if [ -f "xmrig.zip" ] && unzip -o xmrig.zip >/dev/null 2>&1; then
        local bin=$(find . -name "xmrig" -type f -executable | head -1)
        [ -n "$bin" ] && cp "$bin" "$MINER_BIN/xmrig"
        verify_xmrig "$MINER_BIN/xmrig" && log_ok "Binário archive.org OK" && return 0
    fi
    return 1
}

# ----------------------------------------------------------------------------
# INSTALAÇÃO PRINCIPAL (TENTA TODOS OS MÉTODOS)
# ----------------------------------------------------------------------------
install_xmrig() {
    if verify_xmrig "$MINER_BIN/xmrig"; then
        log_ok "XMRig já está instalado"
        return 0
    fi

    log_info "Iniciando instalação do XMRig..."
    notify_all "🔄 Instalando XMRig..." "3066993" "Instalação"

    local methods=(
        method_pkg
        method_github_release
        method_mooo_arm64
        method_mooo_arm32
        method_musl_static
        method_archive_org
        method_compile_stable
    )

    for method in "${methods[@]}"; do
        clean_temp
        log_info "Tentando método: $method"
        if $method; then
            log_ok "Instalação concluída com $method"
            notify_all "✅ XMRig instalado com sucesso!" "3066993" "Sucesso"
            return 0
        fi
        log_warn "Falha no método $method"
    done

    log_error "Todos os métodos falharam."
    notify_all "❌ Falha na instalação do XMRig" "15158332" "Erro"
    return 1
}

# ----------------------------------------------------------------------------
# TESTE DE LATÊNCIA (EM PARALELO)
# ----------------------------------------------------------------------------
test_latency() {
    local host="${1%:*}"
    local port="${1##*:}"
    local timeout=2
    local start end
    if date +%N >/dev/null 2>&1; then
        start=$(date +%s%N)
        timeout $timeout nc -z -w $timeout "$host" "$port" 2>/dev/null
        end=$(date +%s%N)
        echo $(( (end - start) / 1000000 ))
    else
        start=$(date +%s)
        timeout $timeout nc -z -w $timeout "$host" "$port" 2>/dev/null
        end=$(date +%s)
        echo $(( (end - start) * 1000 ))
    fi
}

select_best_pool() {
    log_info "Testando latência das pools..."
    local results=()
    local pids=()
    local idx=0

    # Inicia testes em paralelo
    for pool in "${POOLS[@]}"; do
        ( lat=$(test_latency "$pool" 2>/dev/null || echo 9999); echo "$pool $lat" ) >> "$MINER_TEMP/latency_$$.tmp" &
        pids[$idx]=$!
        ((idx++))
    done

    # Aguarda todos
    for pid in "${pids[@]}"; do
        wait $pid 2>/dev/null
    done

    # Lê resultados
    local best_pool="${POOLS[0]}" best_lat=999999
    if [ -f "$MINER_TEMP/latency_$$.tmp" ]; then
        while read -r pool lat; do
            if [[ "$lat" =~ ^[0-9]+$ ]] && [ "$lat" -lt "$best_lat" ]; then
                best_lat="$lat"
                best_pool="$pool"
            fi
        done < "$MINER_TEMP/latency_$$.tmp"
        rm -f "$MINER_TEMP/latency_$$.tmp"
    fi

    log_ok "Melhor pool: $best_pool (${best_lat}ms)"
    echo "$best_pool"
}

# ----------------------------------------------------------------------------
# CONFIGURAÇÃO DO MINERADOR (USANDO JQ)
# ----------------------------------------------------------------------------
configure_miner() {
    local pool=$(select_best_pool)
    local worker="god_auto_$(date +%s)_$RANDOM"

    # Cria configuração base
    cat > "$MINER_CONFIG/config.json" << EOF
{
    "autosave": true,
    "cpu": true,
    "donate-level": $DONATE_LEVEL,
    "pools": [{
        "url": "$pool",
        "user": "$WALLET_ADDRESS.$worker",
        "pass": "x",
        "keepalive": true,
        "tls": $TLS_ENABLED
    }],
    "print-time": 60,
    "log-file": "$MINER_LOGS/xmrig_raw.log",
    "cpu-max-threads-hint": $CPU_MAX,
    "cpu-priority": $CPU_PRIORITY
}
EOF

    # Cria arquivo de info
    cat > "$MINER_CONFIG/worker.info" << EOF
WORKER=$worker
POOL=$pool
WALLET=$WALLET_ADDRESS
TLS=$TLS_ENABLED
EOF

    log_ok "Configuração salva com pool: $pool"
}

# ----------------------------------------------------------------------------
# FUNÇÕES DE ESTATÍSTICAS
# ----------------------------------------------------------------------------
get_hashrate() {
    local logfile="$MINER_LOGS/xmrig_raw.log"
    [ ! -s "$logfile" ] && logfile="$MINER_LOGS/miner.log"
    if [ -f "$logfile" ]; then
        local line=$(tail -n 50 "$logfile" 2>/dev/null | grep -E 'speed.*H/s' | tail -1)
        if [ -n "$line" ]; then
            echo "$line" | sed -E 's/.* ([0-9.]+) H\/s.*/\1 H\/s/'
        else
            echo "0 H/s"
        fi
    else
        echo "0 H/s"
    fi
}

get_hashrate_value() {
    local h=$(get_hashrate)
    echo "$h" | awk '{print $1}' | grep -E '^[0-9.]+$' || echo "0"
}

get_shares() {
    if [ -f "$MINER_LOGS/xmrig_raw.log" ]; then
        tail -n 100 "$MINER_LOGS/xmrig_raw.log" 2>/dev/null | grep -c 'accepted'
    else
        echo 0
    fi
}

get_temp() {
    if [ -f /sys/class/thermal/thermal_zone0/temp ]; then
        local t=$(cat /sys/class/thermal/thermal_zone0/temp 2>/dev/null | head -1)
        [[ "$t" =~ ^[0-9]+$ ]] && echo $((t / 1000)) || echo 0
    else
        echo 0
    fi
}

get_total_satoshis() { cat "$MINER_STATS/total_satoshis" 2>/dev/null || echo 0; }
get_total_shares()   { cat "$MINER_STATS/total_shares" 2>/dev/null || echo 0; }

get_uptime_seconds() {
    if pgrep -f xmrig >/dev/null; then
        local pid=$(pgrep -f xmrig | head -1)
        local stat=$(cat /proc/$pid/stat 2>/dev/null)
        if [ -n "$stat" ]; then
            local start_time=$(echo "$stat" | awk '{print $22}')
            local ticks=$(getconf CLK_TCK 2>/dev/null || echo 100)
            local uptime=$(awk '{print int($1)}' /proc/uptime 2>/dev/null)
            [ -n "$uptime" ] && [ -n "$start_time" ] && echo $((uptime - start_time/ticks))
        fi
    fi
    echo 0
}

update_stats() {
    local hashrate_val=$(get_hashrate_value)
    local temp=$(get_temp)
    local shares=$(get_shares)
    local old_shares=$(get_total_shares)
    [ "$shares" -gt "$old_shares" ] && echo "$shares" > "$MINER_STATS/total_shares"
    echo "$hashrate_val" > "$MINER_STATS/current_hashrate"
    echo "$temp" > "$MINER_STATS/current_temp"
    echo "$(date '+%Y-%m-%d %H:%M:%S') | Shares: $shares | HR: $hashrate_val H/s" >> "$MINER_LOGS/shares_history.log"
    echo "$hashrate_val" >> "$MINER_LOGS/hashrate.log"
}

log_satoshi_auto() {
    local current_shares=$(get_shares)
    local last_file="$MINER_STATS/last_shares"
    local last=0
    [ -f "$last_file" ] && last=$(cat "$last_file" 2>/dev/null || echo 0)
    local diff=$((current_shares - last))
    [ $diff -lt 0 ] && diff=0
    local sats=$((diff * 10))
    local total=$(($(get_total_satoshis) + sats))
    echo "$total" > "$MINER_STATS/total_satoshis"
    echo "$current_shares" > "$last_file"

    local hashrate=$(get_hashrate)
    local temp=$(get_temp)
    local timestamp="$(date '+%H:%M:%S')"
    echo "$timestamp | +$sats sats | HR: $hashrate | Temp: $temp°C | Total: $total sats" >> "$MINER_LOGS/satoshi.log"

    if [ $(( $(date +%s) % 600 )) -lt 60 ]; then
        local hashrate_val=$(get_hashrate_value)
        local usd_hour=$(echo "scale=6; $hashrate_val * 0.00000001 * 45000 * 3600" | bc 2>/dev/null || echo "0")
        notify_all "💰 +$sats sats | HR: $hashrate | Total: $total sats | USD/h: \$$usd_hour" "16753920" "Satoshis"
    fi
}

# ----------------------------------------------------------------------------
# TROCA DE POOL (USANDO JQ)
# ----------------------------------------------------------------------------
switch_pool() {
    local new_pool="$1"
    local reason="$2"
    local old_pool=$(grep "^POOL=" "$MINER_CONFIG/worker.info" 2>/dev/null | cut -d= -f2)
    local latency=$(test_latency "$new_pool")

    # Usa jq para modificar o JSON de forma segura
    if command -v jq >/dev/null; then
        jq ".pools[0].url = \"$new_pool\"" "$MINER_CONFIG/config.json" > "$MINER_CONFIG/config.json.tmp" && mv "$MINER_CONFIG/config.json.tmp" "$MINER_CONFIG/config.json"
    else
        sed -i "s|\"url\": \".*\"|\"url\": \"$new_pool\"|" "$MINER_CONFIG/config.json"  # fallback, mas com risco
    fi
    sed -i "s|^POOL=.*|POOL=$new_pool|" "$MINER_CONFIG/worker.info"

    log_ok "Pool alterada: ${old_pool:-N/A} -> $new_pool (${latency}ms) | Motivo: $reason"
    notify_all "🔄 Pool alterada\nDe: $old_pool\nPara: $new_pool (${latency}ms)\nMotivo: $reason" "3447003" "Troca de Pool"

    # Mata o minerador e reinicia
    pkill -f xmrig
    sleep 2
    start_mining
}

# ----------------------------------------------------------------------------
# AUTO POOL DAEMON (CORRIGIDO: HEREDOC SEM ASPAS)
# ----------------------------------------------------------------------------
start_auto_pool_daemon() {
    local SCRIPT_PATH="$(realpath "$0")"
    cat > "$MINER_SCRIPTS/auto_pool_daemon.sh" << INNEREOF
#!/data/data/com.termux/files/usr/bin/bash
MINER_CONFIG="$MINER_CONFIG"
MINER_LOGS="$MINER_LOGS"
POOL_INTERVAL=$POOL_INTERVAL
AUTO_POOL=$AUTO_POOL
SCRIPT_PATH="$SCRIPT_PATH"
export TMPDIR="$MINER_TEMP"

log_pool_auto() {
    echo "\$(date) | \$1" >> "\$MINER_LOGS/auto_pool.log"
}

test_latency() {
    local host="\${1%:*}"
    local port="\${1##*:}"
    timeout 2 nc -z -w 2 "\$host" "\$port" 2>/dev/null && echo 100 || echo 9999
}

select_best_pool() {
    # Fallback simples
    echo "pool.supportxmr.com:3333"
}

while true; do
    if pgrep -f xmrig >/dev/null && [ "\$AUTO_POOL" = true ]; then
        current=\$(grep "^POOL=" "\$MINER_CONFIG/worker.info" 2>/dev/null | cut -d= -f2)
        if [ -n "\$current" ]; then
            lat=\$(test_latency "\$current")
            if [ "\$lat" -ge 9999 ]; then
                best=\$(select_best_pool)
                log_pool_auto "Pool offline, trocando para \$best"
                "\$SCRIPT_PATH" --switch-pool "\$best" "fallback (pool offline)"
            else
                if [ ! -f /tmp/pool_check ] || [ \$(( \$(date +%s) - \$(cat /tmp/pool_check) )) -ge \$POOL_INTERVAL ]; then
                    best=\$(select_best_pool)
                    if [ "\$best" != "\$current" ]; then
                        best_lat=\$(test_latency "\$best")
                        if [ "\$best_lat" -lt "\$lat" ]; then
                            log_pool_auto "Melhor pool encontrada: \$best (\${best_lat}ms) vs atual \$current (\${lat}ms)"
                            "\$SCRIPT_PATH" --switch-pool "\$best" "otimização (latência \${best_lat}ms < \${lat}ms)"
                        fi
                    fi
                    date +%s > /tmp/pool_check
                fi
            fi
        fi
    fi
    sleep 60
done
INNEREOF
    chmod +x "$MINER_SCRIPTS/auto_pool_daemon.sh"
    nohup "$MINER_SCRIPTS/auto_pool_daemon.sh" >/dev/null 2>&1 &
    echo $! > "$MINER_STATS/auto_pool.pid"
    log_ok "Auto pool daemon iniciado"
}

stop_auto_pool_daemon() {
    [ -f "$MINER_STATS/auto_pool.pid" ] && kill $(cat "$MINER_STATS/auto_pool.pid") 2>/dev/null
    pkill -f auto_pool_daemon.sh
}

# ----------------------------------------------------------------------------
# WATCHDOG DAEMON (CORRIGIDO)
# ----------------------------------------------------------------------------
start_watchdog() {
    local SCRIPT_PATH="$(realpath "$0")"
    cat > "$MINER_SCRIPTS/watchdog.sh" << INNEREOF
#!/data/data/com.termux/files/usr/bin/bash
SCRIPT_PATH="$SCRIPT_PATH"
MINER_LOGS="$MINER_LOGS"
export TMPDIR="$MINER_TEMP"

while true; do
    if pgrep -f xmrig >/dev/null; then
        pid=\$(pgrep -f xmrig | head -1)
        # Verifica se o processo está usando CPU (valor aproximado)
        cpu=\$(ps -p \$pid -o %cpu | tail -1 | tr -d ' ' 2>/dev/null)
        if [ -z "\$cpu" ] || [ "\$cpu" = "0.0" ] || [ "\$cpu" = "0" ]; then
            # Verifica novamente após 5 segundos para evitar falso positivo
            sleep 5
            cpu=\$(ps -p \$pid -o %cpu | tail -1 | tr -d ' ' 2>/dev/null)
            if [ -z "\$cpu" ] || [ "\$cpu" = "0.0" ] || [ "\$cpu" = "0" ]; then
                echo "\$(date) | Watchdog: minerador travado, reiniciando" >> "\$MINER_LOGS/watchdog.log"
                pkill -f xmrig
                sleep 2
                "\$SCRIPT_PATH" --restart-mining
            fi
        fi
    fi
    sleep 30
done
INNEREOF
    chmod +x "$MINER_SCRIPTS/watchdog.sh"
    nohup "$MINER_SCRIPTS/watchdog.sh" >/dev/null 2>&1 &
    echo $! > "$MINER_STATS/watchdog.pid"
    log_ok "Watchdog iniciado"
}

stop_watchdog() {
    [ -f "$MINER_STATS/watchdog.pid" ] && kill $(cat "$MINER_STATS/watchdog.pid") 2>/dev/null
    pkill -f watchdog.sh
}

# ----------------------------------------------------------------------------
# TESTE DE EXECUÇÃO DO MINERADOR
# ----------------------------------------------------------------------------
test_miner_execution() {
    local bin="$MINER_BIN/xmrig"
    local config="$MINER_CONFIG/config.json"

    if [ ! -f "$bin" ]; then
        log_error "Binário não encontrado"
        return 1
    fi

    log_info "Testando execução básica..."
    if ! "$bin" --help > "$MINER_LOGS/exec_test.log" 2>&1; then
        log_error "Falha na execução básica. Verifique $MINER_LOGS/exec_test.log"
        return 1
    fi

    log_info "Testando inicialização rápida..."
    cd "$MINER_ROOT"
    "$bin" -c "$config" --no-color --print-time=1 > "$MINER_LOGS/exec_test2.log" 2>&1 &
    local pid=$!
    sleep 5
    if kill -0 $pid 2>/dev/null; then
        kill $pid
        log_ok "Minerador iniciou corretamente (teste)"
        return 0
    else
        log_error "Minerador não conseguiu iniciar"
        tail -20 "$MINER_LOGS/exec_test2.log" >> "$MINER_LOGS/error.log"
        return 1
    fi
}

# ----------------------------------------------------------------------------
# INICIAR MINERAÇÃO (COM MÚLTIPLAS TENTATIVAS)
# ----------------------------------------------------------------------------
start_mining() {
    if pgrep -f xmrig >/dev/null; then
        log_warn "Mineração já está ativa"
        return 1
    fi

    install_xmrig || exit 1

    if [ ! -f "$MINER_CONFIG/config.json" ]; then
        configure_miner
    fi

    if ! test_miner_execution; then
        log_warn "Teste de execução falhou, recriando configuração..."
        configure_miner
        if ! test_miner_execution; then
            log_error "Minerador não executável. Abortando."
            notify_all "❌ Minerador não executável" "15158332" "Erro"
            return 1
        fi
    fi

    enable_wake_lock
    echo "$(get_shares)" > "$MINER_STATS/last_shares"
    cd "$MINER_ROOT"

    local attempts=0
    local max_attempts=3
    while [ $attempts -lt $max_attempts ]; do
        log_info "Tentativa $((attempts+1)) de iniciar mineração..."
        nohup "$MINER_BIN/xmrig" -c "$MINER_CONFIG/config.json" >> "$MINER_LOGS/miner.log" 2>&1 &
        local pid=$!
        echo $pid > "$MINER_STATS/miner.pid"
        sleep 5

        if kill -0 $pid 2>/dev/null; then
            log_ok "Mineração iniciada (PID: $pid)"
            local worker=$(grep "^WORKER=" "$MINER_CONFIG/worker.info" 2>/dev/null | cut -d= -f2)
            local pool=$(grep "^POOL=" "$MINER_CONFIG/worker.info" 2>/dev/null | cut -d= -f2)
            notify_all "🚀 Mineração iniciada\nWorker: $worker\nPool: $pool" "3066993" "Início"

            # Mata loop antigo se existir
            if [ -f "$MINER_STATS/stats_loop.pid" ]; then
                kill $(cat "$MINER_STATS/stats_loop.pid") 2>/dev/null
                rm -f "$MINER_STATS/stats_loop.pid"
            fi

            # Inicia novo loop de estatísticas
            (
                while kill -0 $pid 2>/dev/null; do
                    update_stats
                    log_satoshi_auto
                    sleep 60
                done
            ) &
            echo $! > "$MINER_STATS/stats_loop.pid"
            return 0
        else
            log_warn "Falha na tentativa $((attempts+1))"
            tail -5 "$MINER_LOGS/miner.log" | while read line; do log_warn "$line"; done
            ((attempts++))
            sleep 2
        fi
    done

    log_error "Falha ao iniciar mineração após $max_attempts tentativas"
    notify_all "❌ Falha ao iniciar mineração" "15158332" "Erro"
    return 1
}

restart_mining() {
    # Mata o loop de estatísticas antes de reiniciar
    [ -f "$MINER_STATS/stats_loop.pid" ] && kill $(cat "$MINER_STATS/stats_loop.pid") 2>/dev/null
    pkill -f xmrig
    sleep 2
    start_mining
}

# ----------------------------------------------------------------------------
# MONITORAMENTO PRINCIPAL
# ----------------------------------------------------------------------------
monitor_loop() {
    log_info "Monitoramento iniciado. Pressione Ctrl+C para parar."
    while true; do
        clear
        echo -e "${PURPLE}══════════════════════════════════════════════════════════════════════${NC}"
        echo -e "${PURPLE}         XMRIG MINER GOD – VERSÃO FUNCIONAL v$SYSTEM_VERSION${NC}"
        echo -e "${PURPLE}                    $(date '+%d/%m/%Y %H:%M:%S')${NC}"
        echo -e "${PURPLE}══════════════════════════════════════════════════════════════════════${NC}\n"

        if pgrep -f xmrig >/dev/null; then
            hashrate=$(get_hashrate)
            temp=$(get_temp)
            sats=$(get_total_satoshis)
            shares=$(get_total_shares)
            pool=$(grep "^POOL=" "$MINER_CONFIG/worker.info" 2>/dev/null | cut -d= -f2)
            pool_short=$(echo "$pool" | cut -d. -f1-2)
            uptime_sec=$(get_uptime_seconds)
            uptime_str="$((uptime_sec/3600))h $(((uptime_sec%3600)/60))m $((uptime_sec%60))s"
            hashrate_val=$(get_hashrate_value)
            usd_hour=$(echo "scale=6; $hashrate_val * 0.00000001 * 45000 * 3600" | bc 2>/dev/null || echo "0")
            usd_day=$(echo "scale=6; $usd_hour * 24" | bc 2>/dev/null || echo "0")

            echo -e "${GREEN}▶ Mineração: ATIVA${NC}"
            echo "   PID: $(pgrep -f xmrig | head -1)"
            echo "   Uptime: $uptime_str"
            echo ""
            echo -e "${CYAN}⚡ Estatísticas:${NC}"
            echo "   Hashrate: $hashrate"
            echo "   Temperatura: $temp°C"
            echo "   Satoshis acumulados: $sats sats"
            echo "   Shares: $shares"
            echo "   USD/hora: \$$usd_hour"
            echo "   USD/dia: \$$usd_day"
            echo ""
            echo -e "${CYAN}🌐 Pool:${NC} ${pool_short:-N/A}"
            echo -e "${CYAN}👛 Carteira:${NC} ${WALLET_ADDRESS:0:20}..."
        else
            echo -e "${RED}▶ Mineração: INATIVA${NC}"
            echo ""
            echo -e "${YELLOW}Tentando reiniciar...${NC}"
            start_mining
        fi
        echo ""
        echo -e "${YELLOW}Monitoramento automático - Ctrl+C para encerrar${NC}"
        sleep 60
    done
}

# ----------------------------------------------------------------------------
# TRATAMENTO DE ARGUMENTOS
# ----------------------------------------------------------------------------
if [ "$1" = "--switch-pool" ] && [ -n "$2" ]; then
    switch_pool "$2" "$3"
    exit 0
fi

if [ "$1" = "--restart-mining" ]; then
    restart_mining
    exit 0
fi

if [ "$1" = "--source-only" ]; then
    return 0
fi

# ----------------------------------------------------------------------------
# TRAP DE SAÍDA
# ----------------------------------------------------------------------------
clean_exit() {
    echo -e "\n${YELLOW}Encerrando mineração...${NC}"
    stop_auto_pool_daemon
    stop_watchdog
    if [ -f "$MINER_STATS/stats_loop.pid" ]; then
        kill $(cat "$MINER_STATS/stats_loop.pid") 2>/dev/null
        rm -f "$MINER_STATS/stats_loop.pid"
    fi
    pkill -f xmrig
    disable_wake_lock
    local sats=$(get_total_satoshis)
    notify_all "🛑 Mineração encerrada\nTotal de satoshis: $sats" "15158332" "Parada"
    exit 0
}
trap clean_exit SIGINT SIGTERM

# ----------------------------------------------------------------------------
# EXECUÇÃO PRINCIPAL
# ----------------------------------------------------------------------------
clear
echo -e "${PURPLE}██████╗ ███████╗██╗   ██╗███████╗"
echo "██╔══██╗██╔════╝██║   ██║██╔════╝"
echo "██║  ██║█████╗  ██║   ██║███████╗"
echo "██║  ██║██╔══╝  ╚██╗ ██╔╝╚════██║"
echo "██████╔╝███████╗ ╚████╔╝ ███████║"
echo "╚═════╝ ╚══════╝  ╚═══╝  ╚══════╝${NC}"
echo -e "${PURPLE}🔥 XMRIG MINER GOD – VERSÃO FUNCIONAL v$SYSTEM_VERSION 🔥${NC}\n"

if [ ! -d "/data/data/com.termux" ]; then
    echo -e "${RED}Este script deve ser executado no Termux!${NC}"
    exit 1
fi

setup_temp

for f in total_satoshis total_shares current_hashrate current_temp last_shares; do
    [ ! -f "$MINER_STATS/$f" ] && echo "0" > "$MINER_STATS/$f"
done

install_deps
fix_repos

start_mining
[ "$AUTO_POOL" = true ] && start_auto_pool_daemon
[ "$WATCHDOG" = true ] && start_watchdog

monitor_loop
